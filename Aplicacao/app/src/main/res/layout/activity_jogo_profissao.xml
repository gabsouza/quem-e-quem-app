<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/tv_pergunta"
        android:textSize="20dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="40dp"
        android:layout_marginRight="10dp"
        android:layout_marginLeft="10dp"/>

    <ImageView
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:id="@+id/iv_personagem"
        android:layout_gravity="center_horizontal"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="100dp"/>

    <ImageButton
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:id="@+id/bt_falar_pergunta"
        android:textColor="@color/colorPrimaryText"
        android:background="@drawable/borda"
        android:src="@drawable/audio"
        android:scaleType="fitCenter"
        android:layout_alignTop="@+id/iv_personagem"
        android:layout_below="@+id/tv_pergunta"
        android:layout_alignParentEnd="true"/>

    <android.support.v7.widget.RecyclerView
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/rv_alternativas"
        android:clickable="false"
        android:background="@android:color/white"
        android:layout_alignBottom="@+id/iv_personagem"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="135dp">
    </android.support.v7.widget.RecyclerView>


</RelativeLayout>